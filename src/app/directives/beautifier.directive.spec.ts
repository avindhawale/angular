import { BeautifierDirective } from './beautifier.directive';

describe('BeautifierDirective', () => {
  it('should create an instance', () => {
    const directive = new BeautifierDirective();
    expect(directive).toBeTruthy();
  });
});
